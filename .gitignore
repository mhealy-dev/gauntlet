# compiled binary
/gauntlet

# current-glove selection (machine state)
gloves/.current

# Blizzard art — not redistributable (rebuild with scripts/make-wow-glove.sh).
# The wow gloves' config (skip, hotspots.json) is ours and stays tracked;
# only the artwork is excluded.
gloves/wow/
gloves/wow-solid/*.png

# generated cursor reference — Apple's artwork, rebuild with
# scripts/dump-cursors.sh. CURSORS.md carries the reference itself.
reference/

# python scratch
venv/
__pycache__/

# macOS
.DS_Store
