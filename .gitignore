# compiled binary
/gauntlet

# gloves are personal: everything you make stays local, only the bundled
# example ships. (Covers .current, the machine's selected-glove marker.)
gloves/*
!gloves/example/

# local-only tooling
local/

# generated cursor reference — Apple's artwork, rebuild with
# scripts/dump-cursors.sh. CURSORS.md carries the reference itself.
reference/

# python scratch
venv/
__pycache__/

# macOS
.DS_Store
